# Acrobot Trajectory Optimization

Requires [Julia 1.0](https://julialang.org/). Once Julia is installed run `julia required.jl` to install the required packages.
To run the demo, run `julia trajopt.jl`